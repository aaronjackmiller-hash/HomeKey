# Reduce memory & output size during `react-scripts build` on Render free tier.
# These are read by react-scripts at build time; they are NOT embedded in the
# browser bundle and do not expose any secrets.
GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false
